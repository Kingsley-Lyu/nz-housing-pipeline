name: dbt_project
version: '1.0.0'
profile: dbt_project

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  dbt_project:
    staging:
      +schema: STAGING
      +materialized: view
    marts:
      +schema: MARTS
      +materialized: table